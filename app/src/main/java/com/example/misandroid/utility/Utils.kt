package com.example.misandroid.utility

import com.example.misandroid.database.MeasurementEntity

class Utils {
    fun CalculateDistance(userPosition: String, measurements: List<MeasurementEntity>): Pair<List<Int>, Int>{
        val l = List(1) { 1 }
        val i = 1
        return Pair(l, i)
    }
}